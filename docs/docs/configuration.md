---
title: Configuration
layout: default
---

Edit `_config.yml` to change site title, description and `baseurl`.

Use `_data/navigation.yml` to reorder or add pages to the navigation without modifying templates.

Custom styles live in `assets/css/styles.css`. The file uses CSS variables at the top for colors and sizing.
